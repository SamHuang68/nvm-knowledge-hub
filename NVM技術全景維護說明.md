# NVM 技術全景維護說明

此專題直接整合於 NVM Knowledge Hub 主站。網站依本次使用者要求預設英文，並提供完整臺灣繁體中文切換。`NVM技術全景.html` 是英文入口，`NVM技術全景中文.html` 是中文入口。受眾為有半導體背景、尚未熟悉各類 NVM 元件的讀者，內容採中立教學觀點。

資料與簡報共用同一來源。`data` 內五組成對 JSON 分別保存導論、電荷專題、新興專題、比較與系統，以及晶圓代工路線圖；英文檔以「英文」結尾。兩種語言保留相同識別碼、資料結構、來源、數值條件及專利關聯。`scripts/建立NVM技術全景.mjs` 驗證來源與主題欄位，生成兩種靜態網頁、完整資料包、可下載的專題文字及雙語搜尋索引。`scripts/建立知識中心首頁.mjs` 產生首頁。編修時修改來源資料或產生程式，再重新產生。

```powershell
npm.cmd run build:nvm
npm.cmd run check:nvm
npm.cmd run serve
# 在另一個終端機執行瀏覽器檢查
npm.cmd run qa:nvm
npm.cmd run qa:bilingual
npm.cmd run qa:engineering
npm.cmd run qa:mtp
```

後續製作簡報時，從 `data/NVM知識資料英文.json` 或 `data/NVM知識資料.json` 選取同一主題、來源識別碼、日期與限制；亦可使用對應的「NVM技術專題」Markdown 閱讀全文。網站為後續簡報的內容來源，本次不建立新簡報或 PDF。

十六張元件圖由 `scripts/NVM專業元件圖.mjs` 以可編輯 SVG 產生，明確區分材料堆疊、接點、電流及極化狀態。圖例、來源與限制保留在圖旁。元件圖是公開原理重建，不能當作特定晶圓代工公司的實際剖面。手機可開啟大圖並水平移動；圖例獨立於圖面捲動區，大圖須支援 Escape 關閉及焦點返回。

操作教材由 `scripts/NVM電荷操作圖.mjs` 與 `scripts/NVM新興操作圖.mjs` 產生，經 `scripts/NVM工程圖解介面.mjs` 整合。十六個技術與 IP 專題各有寫入、反向操作與讀取，共四十八種操作、六十四組結構或機制變體，每種語言二百四十一格狀態圖。相同圖組保持材料、端點、摻雜、載子方向與偏壓慣例一致；不同來源的抹除出口與井區控制不得拼接。部分圖是明確限定的教學結構，不能當作產品版圖或實測結果。

傳統獨立式 EEPROM 使用 `eeprom` 識別碼，內容聚焦完整元件與介面；嵌入式 MTP IP 使用獨立的 `mtp` 識別碼，內容分列晶圓廠雙層多晶矽 EEPROM 製程選項，以及第三方單層多晶矽 MTP IP。`integrationRoutes` 記錄兩條整合路徑，`implementations` 記錄具名 IP 的結構、寫入、抹除與證據限制。單層多晶矽不代表各家採用相同載子機制；Synopsys、NeoEE 與 NeoMTP 依各自公開資料陳述。MTP 元件圖以 US5844271A 的埋入式控制區作為單層結構教學範例，不能套用至全部現行 IP。既有 EEPROM 操作錨點與專利錨點保留；該專利的主題歸屬改為 MTP。

十九件專利提供二十七張公開原始附圖。`data/NVM專利附圖.json` 記錄圖檔、來源、公報雜湊與附圖位元組雜湊；`scripts/NVM專利圖解.mjs` 維護圖號、PDF 頁次、原始構件編號、權利項技術導讀與操作關聯。公報圖頁保留原始內容，閱讀介面僅調整顯示方向。權利項的限定與實施例細節分開陳述，不由附圖推斷現行產品採用、授權範圍或自由實施結論。

資料包版本為 `1.1`；`engineering.operations` 保留各變體的完整 SVG、狀態、刺激、圖說、符號與來源，`engineering.patents` 保留原圖與導讀。操作圖可逐格下載 SVG，作為後續可編輯簡報圖的來源。放大檢視提供一至三倍倍率、保持圖內閱讀位置，並保留圖例或專利編號說明。`#op-feram-read` 等操作錨點可直接定位，語言切換及重新載入須延續所選操作。

每個技術紀錄保留儲存狀態、結構、操作前後、選擇條件、變異、優缺點、四層限制、成熟度、專利、來源與理解題。商用階段綁定具名實作；公開路線圖中的目標與完成事件分列。歷史課程表供對照，不當作現行規格。來源類別、商用成熟度與系統用途是獨立欄位。

路由使用穩定錨點，例如 `#topic-stt`、`#topic-vcm`、`#foundry`、`#comparison`、`#system-scm`。全站 `site-language.js` 管理語言偏好；首次造訪預設英文，明確的 `?lang=en`／`?lang=zh` 可指定語言。兩種靜態專題頁切換時保留錨點，返回首頁及搜尋結果亦延續偏好。JavaScript 關閉時，兩個專題頁仍各自保有完整正文。

公開入口為 `https://samhuang68.github.io/nvm-knowledge-hub/NVM技術全景.html`。發布採主站既有的 GitHub Pages 流程：先提交內容，再產生並提交發行來源紀錄，推送至 `main` 後核對部署結果與公開檔案。`data/release-lineage.json` 提供內容提交與檔案樹的版本對應；實際上線需另以 GitHub Pages 成功部署及公開頁面驗證。

完整主站檢查使用 `npm.cmd run check`。導覽檢查涵蓋十七個公開 HTML 入口、巢狀目錄、真正存在的錨點與語言路由；動態章節使用 Chromium DOM 查證。新增專題另有雙語、手機與大圖操作檢查。無頭 Chromium 啟動需要依平台規則取得沙箱外執行核准；一般資料產生與字形查核仍在工作區執行。

AI 資料的 JSON 來源使用固定 LF，以保持公開來源位元組雜湊一致；兩個 CSV 固定使用匯出器的 CRLF 格式，確保重新取出儲存庫後仍可重現匯出結果。既有兩個公開簡報附件保留原始位元組，發布政策依確切路徑、既有公開版本及 SHA-256 核對；不允許以相同檔名替換內容後沿用例外。其餘附件與敏感分類仍採原本禁止規則。
